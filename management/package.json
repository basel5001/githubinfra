{
  "name": "@githubinfra/management",
  "version": "1.0.0",
  "description": "GitHub repo management via Octokit SDK",
  "type": "module",
  "scripts": {
    "sync": "tsx src/sync.ts",
    "sync:dry-run": "tsx src/sync.ts --dry-run",
    "diff": "tsx src/diff.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
